# Backend

This folder is for backend code (APIs, server logic, database models, etc).

- Place your Express (or other backend framework) entry point in `src/`.
- Use `routes/`, `controllers/`, `models/`, `middleware/`, and `utils/` as needed.
